import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Us & Join as a Volunteer",
  description: "Get in touch with Dua Charitable Trust to volunteer, collaborate, or request information about our community programs.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
